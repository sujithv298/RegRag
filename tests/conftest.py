"""Shared pytest fixtures.

Frozen XML fixtures for ingestion tests live in tests/fixtures/.
Real Chroma + model API tests are marked `integration` and skipped in CI.
"""

from __future__ import annotations
